using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// Helper script to automatically create battle UI if it doesn't exist
/// </summary>
public class BattleUIHelper : MonoBehaviour {
    void Start() {
        // This will be called to ensure UI exists
        // The actual UI should be set up in the BattleScene manually
        // This is just a placeholder for future auto-creation
    }
}

